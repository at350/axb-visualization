import React from 'react'
import ReactDOM from 'react-dom/client'
import LinearSystemVisualizer from './LinearSystemVisualizer.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LinearSystemVisualizer />
  </React.StrictMode>,
)
